//
// Created by ZJZ0 on 24-7-14.
//

#include "SystemClock.hpp"


void SystemClock_Init(void)
{
//     LOG_INFO("start test");
//     const auto t = SystemClock::clock::now();
//
//     const auto t2 = SystemClock::clock::now();
// //
//     LOG_INFO("%d", std::chrono::duration_cast<SystemClock::clock::milisecond>(t2-t).count());
}
void SystemClock_Task(void)
{
//     const auto t = SystemClock::clock::now();
//     uint32_t a = DWT_CYCCNT();
//     for (int i = 0; i < 100000; ++i) {}
//     const auto t2 = SystemClock::clock::now();
//     uint32_t b = DWT_CYCCNT();
//     LOG_INFO("1: %d", (b-a)/168u);
// //
//     LOG_INFO("2: %d", std::chrono::duration_cast<SystemClock::clock::milisecond>(t2-t).count());
}
